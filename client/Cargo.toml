[package]
name = "client"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "client"
path = "src/main.rs"

[dependencies]

bevy = { version = "0.13", features = [] }
# 
games = { path = "../games", default-features = false, features = ["client"] }
